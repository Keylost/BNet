#include <stdio.h>
#include "trainDataCollection.hpp"
#include "net.hpp"
#include "timer.hpp"
#include <opencv2/opencv.hpp>

using namespace std;
using namespace cv;

int main(int argc, char** argv)
{
	//trainDataCollection tr;
	//tr.loadImagesFromLst("../DB/samples/mix_rnd.txt",16,16);
	
	//nn.addInputLayer(tr.inputsCount);
	//nn.addHiddenLayer(100,LOGISTIC);
	//nn.addOutputLayer(tr.outputsCount,LOGISTIC);
	
	//nn.train(tr);
	
	//nn.saveModel("../models/model_sgns.mdl");	
	
	return 0;
}
